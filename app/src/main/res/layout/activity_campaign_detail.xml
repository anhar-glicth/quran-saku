<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_dark_background">

    <!-- AppBar with collapsing banner -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:fitsSystemWindows="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:contentScrim="@color/accent_color">

            <ImageView
                android:id="@+id/img_campaign_detail_banner"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/bg_home_verse_card"
                android:fitsSystemWindows="true"
                app:layout_collapseMode="parallax"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/gradient_bottom_dark"
                app:layout_collapseMode="parallax"/>

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar_campaign_detail"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:layout_collapseMode="pin"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>
        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Scrollable Content -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Campaign Title -->
            <TextView
                android:id="@+id/tv_campaign_detail_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Judul Campaign"
                android:textColor="@color/title_color"
                android:textSize="22sp"
                android:textStyle="bold"
                android:layout_marginBottom="12dp"/>

            <!-- Divider -->
            <View
                android:layout_width="40dp"
                android:layout_height="3dp"
                android:background="@color/accent_color"
                android:layout_marginBottom="20dp"/>

            <!-- Deskripsi -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tentang Campaign"
                android:textColor="@color/title_color"
                android:textSize="15sp"
                android:textStyle="bold"
                android:layout_marginBottom="10dp"/>

            <TextView
                android:id="@+id/tv_campaign_detail_desc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Deskripsi campaign..."
                android:textColor="@color/text_primary_color"
                android:textSize="14sp"
                android:lineSpacingMultiplier="1.7"
                android:layout_marginBottom="32dp"/>

            <!-- Tombol Donasi -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_donate_now"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="💝 Donasi Sekarang"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                app:cornerRadius="16dp"
                android:backgroundTint="@color/accent_color"
                android:layout_marginBottom="12dp"/>

            <!-- Info link donasi -->
            <TextView
                android:id="@+id/tv_donate_link_info"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Anda akan diarahkan ke halaman donasi eksternal"
                android:textColor="@color/text_hint_color"
                android:textSize="11sp"
                android:gravity="center"
                android:layout_marginBottom="24dp"/>

            <!-- Admin buttons -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_edit_campaign_detail"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="✏️ Edit Campaign"
                android:textSize="14sp"
                android:layout_marginTop="4dp"
                app:cornerRadius="12dp"
                android:backgroundTint="#33FF6D00"
                android:textColor="@color/accent_color"
                android:visibility="gone"
                app:strokeColor="@color/accent_color"
                app:strokeWidth="1dp"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_delete_campaign_detail"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="🗑️ Hapus Campaign"
                android:textSize="14sp"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="20dp"
                app:cornerRadius="12dp"
                android:backgroundTint="#33F44336"
                android:textColor="#F44336"
                android:visibility="gone"
                app:strokeColor="#F44336"
                app:strokeWidth="1dp"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
