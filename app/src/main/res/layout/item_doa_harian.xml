<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="14dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#FFEAEAEA"
    app:cardBackgroundColor="#FFFFFF">

  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="vertical"
      android:padding="18dp">

    <!-- Header: kategori badge + judul -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="14dp">

      <TextView
          android:id="@+id/tv_doa_title"
          android:layout_width="0dp"
          android:layout_height="wrap_content"
          android:layout_toStartOf="@id/tv_doa_category"
          android:layout_marginEnd="8dp"
          android:text="Doa Sebelum Makan"
          android:textColor="#212121"
          android:textSize="14sp"
          android:textStyle="bold"
          android:layout_alignParentStart="true"
          android:layout_centerVertical="true"/>

      <TextView
          android:id="@+id/tv_doa_category"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_alignParentEnd="true"
          android:layout_centerVertical="true"
          android:paddingHorizontal="10dp"
          android:paddingVertical="4dp"
          android:text="Harian"
          android:textColor="@color/accent_color"
          android:textSize="10sp"
          android:textStyle="bold"
          android:background="@drawable/bg_chip_accent"/>
    </RelativeLayout>

    <!-- Teks Arab -->
    <TextView
        android:id="@+id/tv_doa_arabic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="بِسْمِ اللَّهِ"
        android:textColor="#1A1A1A"
        android:textSize="24sp"
        android:gravity="end"
        android:fontFamily="sans-serif"
        android:lineSpacingMultiplier="1.6"
        android:layout_marginBottom="14dp"/>

    <!-- Divider tipis -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#F0F0F0"
        android:layout_marginBottom="12dp"/>

    <!-- Latin -->
    <TextView
        android:id="@+id/tv_doa_latin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Bismillah"
        android:textColor="#555555"
        android:textSize="13sp"
        android:fontFamily="sans-serif-medium"
        android:lineSpacingMultiplier="1.4"
        android:layout_marginBottom="8dp"/>

    <!-- Terjemahan -->
    <TextView
        android:id="@+id/tv_doa_translation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dengan nama Allah."
        android:textColor="#888888"
        android:textSize="12sp"
        android:lineSpacingMultiplier="1.4"
        android:layout_marginBottom="10dp"/>

    <!-- Sumber -->
    <TextView
        android:id="@+id/tv_doa_source"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="HR. Bukhari"
        android:textColor="#AAAAAA"
        android:textSize="10sp"
        android:textStyle="italic"/>

  </LinearLayout>
</com.google.android.material.card.MaterialCardView>
